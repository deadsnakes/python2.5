From: Felix Krull <f_krull@gmx.de>
Date: Fri, 27 Apr 2018 08:24:05 -0700
Subject: linecache

---
 Lib/linecache.py | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/Lib/linecache.py b/Lib/linecache.py
index 4838625..748dc51 100644
--- a/Lib/linecache.py
+++ b/Lib/linecache.py
@@ -104,7 +104,11 @@ def updatecache(filename, module_globals=None):
                         )
                         return cache[filename][2]
 
-        # Try looking through the module search path.
+        # Try looking through the module search path, taking care to handle packages.
+
+        if basename == '__init__.py':
+            # filename referes to a package
+            basename = filename
 
         for dirname in sys.path:
             # When using imputil, sys.path may contain things other than
