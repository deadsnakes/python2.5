# Add support for PEP 370, per-user site-packages directories.
import os; import site; import sys; user_site = os.path.expanduser("~/.local/lib/python%s.%s/site-packages" % sys.version_info[:2]); site.addsitedir(user_site) if os.path.isdir(user_site) and getattr(site, "ENABLE_USER_SITE", True) and "PYTHONNOUSERSITE" not in os.environ and os.getuid() == os.geteuid() and os.getgid() == os.getegid() else None
